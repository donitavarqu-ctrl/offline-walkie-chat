name: offline_walkie_chat
description: "Offline Bluetooth Chat and Voice app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  nearby_connections: ^3.3.0
  permission_handler: ^11.3.1
  record: ^5.1.2
  audioplayers: ^6.0.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
  
